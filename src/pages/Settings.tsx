import{useEffect,useState}from'react';import{supabase}from'../lib/supabase';const fields=['nome_empresa','razao_social','cnpj','telefone','email','endereco','cidade','estado','pix','banco','agencia','conta','logo_url'];export default function Settings(){const[data,setData]=useState<Record<string,string>>({}),[msg,setMsg]=useState('');useEffect(()=>{supabase.from('configuracoes_empresa').select('*').limit(1).maybeSingle().then(({data})=>setData(data||{}))},[]);async function save(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const value=Object.fromEntries(new FormData(e.currentTarget));data.id?await supabase.from('configuracoes_empresa').update(value).eq('id',data.id):await supabase.from('configuracoes_empresa').insert(value);setMsg('Configurações salvas.')}return <form onSubmit={save} className="max-w-4xl rounded-2xl border bg-white p-6"><h2 className="text-2xl font-bold">Dados da empresa</h2><div className="mt-6 grid gap-4 sm:grid-cols-2">{fields.map(f=><label className="text-sm font-medium capitalize" key={f}>{f.replaceAll('_',' ')}<input name={f} defaultValue={data[f]||''} className="mt-1.5 w-full rounded-xl border p-3"/></label>)}</div>{msg&&<p className="mt-4 text-emerald-600">{msg}</p>}<button className="mt-6 rounded-xl bg-blue-600 px-5 py-2.5 text-white">Salvar configurações</button></form>}
