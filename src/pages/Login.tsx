import{useState}from'react';import{Navigate}from'react-router-dom';import{supabase,isConfigured}from'../lib/supabase';import{useAuth}from'../lib/auth';import{LockKeyhole,MonitorPlay}from'lucide-react';export default function Login(){const{session,enterDemo}=useAuth(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);if(session)return <Navigate to="/"/>;async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const{error}=await supabase.auth.signInWithPassword({email,password});setError(error?.message||'');setBusy(false)}return <div className="grid min-h-screen bg-slate-950 lg:grid-cols-2"><section className="hidden flex-col justify-between bg-gradient-to-br from-blue-700 to-slate-950 p-14 text-white lg:flex"><div className="text-xl font-bold">MW TECH</div><div><p className="mb-4 text-sm uppercase tracking-[.25em] text-blue-200">Financeiro</p><h1 className="text-5xl font-bold leading-tight">Decisões melhores começam com números claros.</h1><p className="mt-5 text-blue-100">Receitas, despesas, clientes e resultados em um só lugar.</p></div><p className="text-sm text-blue-200">© 2026 MW TECH</p></section><section className="grid place-items-center bg-slate-50 p-6"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border bg-white p-8 shadow-xl"><div className="mb-8 grid size-12 place-items-center rounded-2xl bg-blue-50 text-blue-600"><LockKeyhole/></div><h2 className="text-2xl font-bold">Bem-vindo</h2><p className="mt-2 text-sm text-slate-500">Entre com sua conta administrativa.</p>{!isConfigured&&<div className="mt-5 rounded-xl border border-blue-100 bg-blue-50 p-4"><p className="text-sm text-blue-900">Você pode conhecer o sistema sem configurar o Supabase.</p><button type="button" onClick={enterDemo} className="mt-3 flex w-full items-center justify-center gap-2 rounded-xl bg-blue-600 py-3 font-semibold text-white"><MonitorPlay size={18}/>Entrar no modo demonstração</button></div>}<label className="mt-6 block text-sm font-medium">E-mail<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} disabled={!isConfigured} className="mt-2 w-full rounded-xl border px-4 py-3 disabled:bg-slate-100"/></label><label className="mt-4 block text-sm font-medium">Senha<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} disabled={!isConfigured} className="mt-2 w-full rounded-xl border px-4 py-3 disabled:bg-slate-100"/></label>{error&&<p className="mt-4 text-sm text-red-600">{error}</p>}<button disabled={busy||!isConfigured} className="mt-6 w-full rounded-xl bg-slate-900 py-3 font-semibold text-white disabled:opacity-40">{busy?'Entrando...':'Entrar'}</button></form></section></div>}
