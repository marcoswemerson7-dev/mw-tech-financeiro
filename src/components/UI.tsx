import type{ReactNode}from'react';export const money=(v:unknown)=>Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
export function Card({title,value,icon,tone='navy',hint}:{title:string;value:string;icon?:ReactNode;tone?:'navy'|'green'|'red'|'copper';hint?:string}){const c={navy:'bg-[#eef2f8] text-[#13233d]',green:'bg-emerald-50 text-emerald-600',red:'bg-rose-50 text-rose-600',copper:'bg-[#fff4ee] text-[#b56847]'}[tone];return <div className="group relative overflow-hidden rounded-2xl border border-slate-200/80 bg-white p-5 shadow-[0_4px_20px_rgba(15,23,42,.045)] transition duration-300 hover:-translate-y-0.5 hover:shadow-[0_12px_32px_rgba(15,23,42,.08)]"><div className="absolute inset-x-0 top-0 h-[3px] bg-gradient-to-r from-transparent via-[#c77851]/70 to-transparent opacity-0 transition group-hover:opacity-100"/><div className="flex items-start justify-between"><div><p className="text-[13px] font-medium text-slate-500">{title}</p><strong className="mt-3 block text-[25px] font-bold tracking-[-.03em] text-[#101828]">{value}</strong>{hint&&<p className="mt-2 text-[11px] text-slate-400">{hint}</p>}</div><span className={`grid size-11 place-items-center rounded-xl ${c}`}>{icon}</span></div></div>}
export function Badge({status}:{status:string}){const s=status==='pago'||status==='ativo'?'bg-emerald-50 text-emerald-700 ring-emerald-600/10':status==='atrasado'||status==='suspenso'?'bg-rose-50 text-rose-700 ring-rose-600/10':'bg-amber-50 text-amber-700 ring-amber-600/10';return <span className={`inline-flex rounded-full px-2.5 py-1 text-[11px] font-semibold capitalize ring-1 ring-inset ${s}`}>{status}</span>};export const Empty=()=> <div className="py-16 text-center"><div className="mx-auto mb-3 grid size-10 place-items-center rounded-full bg-slate-100 text-slate-400">—</div><p className="text-sm text-slate-500">Nenhum registro encontrado.</p></div>
