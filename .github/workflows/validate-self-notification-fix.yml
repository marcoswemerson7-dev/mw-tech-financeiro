name: Validate support self-notification fix

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/validate-self-notification-fix.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm install
      - run: npm run build
