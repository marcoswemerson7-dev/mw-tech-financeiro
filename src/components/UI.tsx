import type{ReactNode}from'react';export const money=(v:unknown)=>Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});export function Card({title,value,icon}:{title:string;value:string;icon?:ReactNode}){return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between text-sm text-slate-500"><span>{title}</span><span className="rounded-xl bg-blue-50 p-2 text-blue-600">{icon}</span></div><strong className="mt-3 block text-2xl">{value}</strong></div>}export function Badge({status}:{status:string}){const s=status==='pago'||status==='ativo'?'bg-emerald-100 text-emerald-700':status==='atrasado'||status==='suspenso'?'bg-red-100 text-red-700':'bg-amber-100 text-amber-700';return <span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${s}`}>{status}</span>}export const Empty=()=> <div className="py-14 text-center text-sm text-slate-500">Nenhum registro encontrado.</div>
